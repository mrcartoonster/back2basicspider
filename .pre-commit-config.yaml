# View pre-commit hooks at https://github.com/pre-commit/pre-commit-hooks#hooks-available
default_language_version:
  python: python3.8

default_stages: [commit, push]

fail_fast: true

minimum_pre_commit_version: '1.15.0'

repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v3.1.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
    -   id: check-added-large-files
    -   id: check-case-conflict
    -   id: check-docstring-first
    -   id: check-merge-conflict
    -   id: check-toml
    -   id: debug-statements
    -   id: fix-encoding-pragma
    -   id: detect-private-key
    -   id: forbid-new-submodules
    -   id: name-tests-test
    -   id: pretty-format-json
        args:
          - --autofix
          - --indent 4
          - --no-ensure-ascii
          - --no-sort-keys
-   repo: https://github.com/psf/black
    rev: 19.10b0
    hooks:
    -   id: black
        args:
          - --target-version=py38
          - --line-length=79
          - --verbose
-  repo: https://github.com/timothycrosley/isort
   rev: 4.3.21
   hooks:
   -   id: isort
       args:
         - --trailing-comma
         - --use-parentheses
         - --balanced
# Review pre-commit to add interrogate
# - repo: https://github.com/econchick/interrogate
#   rev: 1.2.0  # or master if you're bold
#   hooks:
#   -   id: interrogate
#       args:
#         - -vv
#         - --exclude=/home/mrnobody/projects/back2basicspider/basics/middlewares.py
